IMPORT("path")

str marker = path.TEMPFILE("prefix-import-path-cache-target.txt")

IF(EXISTFILE(marker)){
    WRITEFILE("reloaded", marker)
} ELSE {
    WRITEFILE("loaded", marker)
}

func str marker_text(){
    RETURN(READFILE(marker))
}