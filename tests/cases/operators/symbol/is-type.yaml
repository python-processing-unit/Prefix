passing:
  - name: isbool
    language: Prefix
    source: |
        bool sample_bool_true = true
        bool sample_bool_false = false
        int sample_int = -0d7
        float sample_flt = 0d3.5
        str sample_str = "prefix"
        tensor sample_tns = [0d1, 0d2]
        map sample_map = <"n" = 0d1>
        
        func int sample_func(int value){
            return(value)
        }
        
        thread(sample_thr){}
        
        assert(isbool(sample_bool_true))
        assert(isbool(sample_bool_false))
        refute(isbool(sample_int))
        refute(isbool(sample_flt))
        refute(isbool(sample_str))
        refute(isbool(sample_tns))
        refute(isbool(sample_map))
        refute(isbool(sample_func))
        refute(isbool(sample_thr))
        
  - name: isflt
    language: Prefix
    source: |
        bool sample_bool_true = true
        int sample_int = -0d7
        float sample_flt = 0d3.5
        str sample_str = "prefix"
        tensor sample_tns = [0d1, 0d2]
        map sample_map = <"n" = 0d1>
        
        func int sample_func(int value){
            return(value)
        }
        
        thread(sample_thr){}
        
        refute(isfloat(sample_bool_true))
        refute(isfloat(sample_int))
        assert(isfloat(sample_flt))
        refute(isfloat(sample_str))
        refute(isfloat(sample_tns))
        refute(isfloat(sample_map))
        refute(isfloat(sample_func))
        refute(isfloat(sample_thr))
        
  - name: isfunc
    language: Prefix
    source: |
        bool sample_bool_true = true
        bool sample_bool_false = false
        int sample_int = -0d7
        float sample_flt = 0d3.5
        str sample_str = "prefix"
        tensor sample_tns = [0d1, 0d2]
        map sample_map = <"n" = 0d1>
        
        func int sample_func(int value){
            return(value)
        }
        
        thread(sample_thr){}
        
        assert(isfunc(sample_func))
        refute(isfunc(sample_bool_true))
        refute(isfunc(sample_bool_false))
        refute(isfunc(sample_int))
        refute(isfunc(sample_flt))
        refute(isfunc(sample_str))
        refute(isfunc(sample_tns))
        refute(isfunc(sample_map))
        refute(isfunc(sample_thr))
        
  - name: isint
    language: Prefix
    source: |
        bool sample_bool_true = true
        int sample_int = -0d7
        float sample_flt = 0d3.5
        str sample_str = "prefix"
        tensor sample_tns = [0d1, 0d2]
        map sample_map = <"n" = 0d1>
        
        func int sample_func(int value){
            return(value)
        }
        
        thread(sample_thr){}
        
        refute(isint(sample_bool_true))
        assert(isint(sample_int))
        refute(isint(sample_flt))
        refute(isint(sample_str))
        refute(isint(sample_tns))
        refute(isint(sample_map))
        refute(isint(sample_func))
        refute(isint(sample_thr))
        
  - name: ismap
    language: Prefix
    source: |
        bool sample_bool_true = true
        bool sample_bool_false = false
        int sample_int = -0d7
        float sample_flt = 0d3.5
        str sample_str = "prefix"
        tensor sample_tns = [0d1, 0d2]
        map sample_map = <"n" = 0d1>
        
        func int sample_func(int value){
            return(value)
        }
        
        thread(sample_thr){}
        
        assert(ismap(sample_map))
        refute(ismap(sample_bool_true))
        refute(ismap(sample_bool_false))
        refute(ismap(sample_int))
        refute(ismap(sample_flt))
        refute(ismap(sample_str))
        refute(ismap(sample_tns))
        refute(ismap(sample_func))
        refute(ismap(sample_thr))
        
  - name: isstr
    language: Prefix
    source: |
        bool sample_bool_true = true
        int sample_int = -0d7
        float sample_flt = 0d3.5
        str sample_str = "prefix"
        tensor sample_tns = [0d1, 0d2]
        map sample_map = <"n" = 0d1>
        
        func int sample_func(int value){
            return(value)
        }
        
        thread(sample_thr){}
        
        refute(isstr(sample_bool_true))
        refute(isstr(sample_int))
        refute(isstr(sample_flt))
        assert(isstr(sample_str))
        refute(isstr(sample_tns))
        refute(isstr(sample_map))
        refute(isstr(sample_func))
        refute(isstr(sample_thr))
        
  - name: isthr
    language: Prefix
    source: |
        bool sample_bool_true = true
        int sample_int = -0d7
        float sample_flt = 0d3.5
        str sample_str = "prefix"
        tensor sample_tns = [0d1, 0d2]
        map sample_map = <"n" = 0d1>
        
        func int sample_func(int value){
            return(value)
        }
        
        thread(sample_thr){}
        
        refute(isthread(sample_bool_true))
        refute(isthread(sample_int))
        refute(isthread(sample_flt))
        refute(isthread(sample_str))
        refute(isthread(sample_tns))
        refute(isthread(sample_map))
        refute(isthread(sample_func))
        assert(isthread(sample_thr))
        
  - name: istns
    language: Prefix
    source: |
        bool sample_bool_true = true
        int sample_int = -0d7
        float sample_flt = 0d3.5
        str sample_str = "prefix"
        tensor sample_tns = [0d1, 0d2]
        map sample_map = <"n" = 0d1>
        
        func int sample_func(int value){
            return(value)
        }
        
        thread(sample_thr){}
        
        refute(istensor(sample_bool_true))
        refute(istensor(sample_int))
        refute(istensor(sample_flt))
        refute(istensor(sample_str))
        assert(istensor(sample_tns))
        refute(istensor(sample_map))
        refute(istensor(sample_func))
        refute(istensor(sample_thr))
        
failing:
  - name: isfunc-arity-two
    language: Prefix
    source: 'isfunc(true, false)'
  - name: isfunc-arity-zero
    language: Prefix
    source: isfunc()
  - name: ismap-arity-two
    language: Prefix
    source: 'ismap(true, false)'
  - name: ismap-arity-zero
    language: Prefix
    source: ismap()
  - name: isthr-arity-two
    language: Prefix
    source: |
        isthread(true, false)
        
  - name: isthr-arity-zero
    language: Prefix
    source: |
        isthread()
        
