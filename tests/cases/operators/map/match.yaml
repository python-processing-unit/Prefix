passing:
  - name: match-basic
    language: Prefix
    source: |
        map candidate = <"alpha" = 0d1, "beta" = "dog">
        
        assert(validate(candidate, <>))
        assert(validate(candidate, <"alpha" = 0d99>))
        assert(validate(candidate, <"alpha" = "ignored", "beta" = 0d0>))
        refute(validate(candidate, <"gamma" = 0d0>))
        assert(eq(type(validate(candidate, <"alpha" = 0d0>)), "bool"))
        
  - name: match-metadata
    language: Prefix
    source: |
        map typed_candidate = <"value" = 0d1, "other" = "text">
        map typed_schema = <"validate" = <"typing" = 0d1>, "value" = 0d1>
        map typed_mismatch_schema = <"validate" = <"typing" = 0d1>, "value" = "ignored">
        
        map missing_candidate = <"outer" = <"present" = 0d1>>
        map missing_schema = <"validate" = <"recurse" = 0d1>, "outer" = <"missing" = 0d1>>
        
        map shape_candidate = <"tensor" = [[0d1, 0d2], [0d3, 0d4]]>
        map shape_schema = <"validate" = <"shape" = 0d1>, "tensor" = [0d9, 0d8, 0d7, 0d6]>
        
        map explicit_typing_candidate = <"value" = 0d1>
        map explicit_typing_schema = <"validate" = <"typing" = 0d1>, "value" = "ignored">
        
        map explicit_recurse_candidate = <"outer" = <"present" = 0d1>>
        map explicit_recurse_schema = <"validate" = <"recurse" = 0d1>, "outer" = <"missing" = 0d1>>
        
        map explicit_shape_candidate = <"tensor" = [[0d1, 0d2], [0d3, 0d4]]>
        map explicit_shape_schema = <"validate" = <"shape" = 0d1>, "tensor" = [0d9, 0d8, 0d7, 0d6]>
        
        map nested_candidate = <"outer" = <"value" = 0d1>>
        map nested_schema = <"outer" = <"validate" = <"typing" = 0d1>, "value" = 0d1>>
        map nested_mismatch_schema = <"outer" = <"validate" = <"typing" = 0d1>, "value" = "ignored">>
        
        map normal_match_candidate = <"match" = "same", "other" = 0d1>
        map normal_match_schema = <"match" = "same">
        
        assert(validate(typed_candidate, typed_schema))
        refute(validate(typed_candidate, typed_mismatch_schema))
        assert(validate(typed_candidate, <"validate" = <"typing" = 0d0>, "value" = "ignored">))
        refute(validate(missing_candidate, missing_schema))
        refute(validate(shape_candidate, shape_schema))
        
        assert(validate(explicit_typing_candidate, explicit_typing_schema, typing = 0d0))
        refute(validate(explicit_typing_candidate, explicit_typing_schema, typing = 0d1))
        assert(validate(explicit_recurse_candidate, explicit_recurse_schema, recurse = 0d0))
        refute(validate(explicit_recurse_candidate, explicit_recurse_schema, recurse = 0d1))
        assert(validate(explicit_shape_candidate, explicit_shape_schema, shape = 0d0))
        refute(validate(explicit_shape_candidate, explicit_shape_schema, shape = 0d1))
        
        assert(validate(nested_candidate, nested_schema, recurse = 0d1))
        refute(validate(nested_candidate, nested_mismatch_schema, recurse = 0d1))
        assert(validate(nested_candidate, nested_mismatch_schema, recurse = 0d1, typing = 0d0))
        assert(validate(nested_candidate, nested_mismatch_schema, recurse = 0d0))
        
        assert(validate(normal_match_candidate, normal_match_schema))
        refute(validate(<"other" = 0d1>, normal_match_schema))
        
        refute(validate(<>, <"match" = "not metadata">))
        refute(validate(<>, <"validate" = <"typing" = "bad">>))
        refute(validate(<>, <"validate" = <"unknown" = 0d1>>))
        assert(validate(<>, <"validate" = <"typing" = -0d1>>))
        assert(validate(<>, <"validate" = <>>))
        
  - name: match-recurse
    language: Prefix
    source: |
        map missing_candidate = <"outer" = <"present" = 0d1>>
        map missing_schema = <"outer" = <"missing" = 0d1>>
        
        map type_candidate = <"outer" = <"value" = 0d1>>
        map type_schema = <"outer" = <"value" = "x">>
        
        map shape_candidate = <"outer" = <"tensor" = [[0d1, 0d2], [0d3, 0d4]]>>
        map shape_schema = <"outer" = <"tensor" = [0d9, 0d8, 0d7, 0d6]>>
        
        assert(validate(missing_candidate, missing_schema))
        refute(validate(missing_candidate, missing_schema, recurse = 0d1))
        
        assert(validate(type_candidate, type_schema, typing = 0d1))
        assert(validate(type_candidate, type_schema, recurse = 0d1))
        refute(validate(type_candidate, type_schema, typing = 0d1, recurse = -0d1))
        
        assert(validate(shape_candidate, shape_schema, shape = 0d1))
        assert(validate(shape_candidate, shape_schema, recurse = 0d1))
        refute(validate(shape_candidate, shape_schema, shape = 0d1, recurse = 0d1))
        
  - name: match-shape
    language: Prefix
    source: |
        map candidate = <"tensor" = [[0d1, 0d2], [0d3, 0d4]], "scalar" = 0d1>
        
        assert(validate(candidate, <"tensor" = [0d9, 0d8, 0d7, 0d6]>))
        assert(validate(candidate, <"tensor" = [[0d9, 0d8], [0d7, 0d6]]>, shape = 0d1))
        refute(validate(candidate, <"tensor" = [0d9, 0d8, 0d7, 0d6]>, shape = -0d1))
        assert(validate(candidate, <"scalar" = "different type">, shape = 0d1))
        
  - name: match-typing
    language: Prefix
    source: |
        map candidate = <"value" = 0d1, "other" = "text">
        
        assert(validate(candidate, <"value" = "1">))
        assert(validate(candidate, <"value" = 0d99>, typing = 0d1))
        assert(validate(candidate, <"other" = "different">, typing = 0d7))
        refute(validate(candidate, <"value" = "1">, typing = -0d1))
        
failing:
  - name: match-arity-one
    language: Prefix
    source: |
        validate(<>)
        
  - name: match-arity-six
    language: Prefix
    source: |
        validate(<>, <>, 0d1, 0d1, 0d1, 0d1)
        
  - name: match-arity-zero
    language: Prefix
    source: |
        validate()
        
  - name: match-first-bool
    language: Prefix
    source: |
        validate(true, <>)
        
  - name: match-first-flt
    language: Prefix
    source: |
        validate(0d1.5, <>)
        
  - name: match-first-func
    language: Prefix
    source: |
        validate(LAMBDA int: (){}, <>)
        
  - name: match-first-int
    language: Prefix
    source: |
        validate(0d1, <>)
        
  - name: match-first-str
    language: Prefix
    source: |
        validate("map", <>)
        
  - name: match-first-thr
    language: Prefix
    source: |
        validate(async{}, <>)
        
  - name: match-first-tns
    language: Prefix
    source: |
        validate([0d1], <>)
        
  - name: match-recurse-not-int
    language: Prefix
    source: |
        validate(<>, <>, recurse = "1")
        
  - name: match-second-bool
    language: Prefix
    source: |
        validate(<>, true)
        
  - name: match-second-flt
    language: Prefix
    source: |
        validate(<>, 0d1.5)
        
  - name: match-second-func
    language: Prefix
    source: |
        validate(<>, LAMBDA int: (){})
        
  - name: match-second-int
    language: Prefix
    source: |
        validate(<>, 0d1)
        
  - name: match-second-str
    language: Prefix
    source: |
        validate(<>, "map")
        
  - name: match-second-thr
    language: Prefix
    source: |
        validate(<>, async{})
        
  - name: match-second-tns
    language: Prefix
    source: |
        validate(<>, [0d1])
        
  - name: match-shape-not-int
    language: Prefix
    source: |
        validate(<>, <>, shape = "1")
        
  - name: match-typing-not-int
    language: Prefix
    source: |
        validate(<>, <>, typing = "1")
        
