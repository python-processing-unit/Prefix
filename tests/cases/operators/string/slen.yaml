passing:
  - name: slen
    language: Prefix
    source: |
        assert(eq(slen(""), 0d0))
        assert(eq(slen("prefix"), 0d6))
        assert(eq(slen("\u0041"), 0d1))
        assert(eq(slen("\u00E9"), 0d1))
        assert(eq(slen("\U0001F600"), 0d1))
        assert(eq(slen("\u0041\u00E9"), 0d2))
failing:
  - name: slen-non-str
    language: Prefix
    source: slen(0d1)
