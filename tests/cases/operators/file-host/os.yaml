passing:
  - name: os-lowercase
    language: Prefix
    source: 'assert(eq(os(), lowercase(os())))'
  - name: os-nonempty
    language: Prefix
    source: 'assert(gt(slen(os()), 0d0))'
  - name: os-return-type
    language: Prefix
    source: 'assert(eq(type(os()), "str"))'
  - name: os
    language: Prefix
    source: 'assert(in(os(), ["win", "linux", "macos", "unix"]))'
failing:
  - name: os-arity-one
    language: Prefix
    source: os("win")
