passing:
  - name: deletefile
    language: Prefix
    source: |
        import("path")
        
        str p = path.temp_file("temp.txt")
        write_file("Prefix", p)
        assert(exist_file(p))
        assert(delete_file(p))
        refute(exist_file(p))
failing:
  - name: deletefile-arity-two
    language: Prefix
    source: |
        import("path")
        
        str p = path.temp_file("temp.txt")
        write_file("Prefix", p)
        assert(exist_file(p))
        assert(delete_file(p, "Prefix"))
  - name: deletefile-arity-zero
    language: Prefix
    source: delete_file()
  - name: deletefile-bool
    language: Prefix
    source: delete_file(true)
  - name: deletefile-flt
    language: Prefix
    source: delete_file(0d1.0)
  - name: deletefile-func
    language: Prefix
    source: 'delete_file(LAMBDA bool: (){})'
  - name: deletefile-int
    language: Prefix
    source: delete_file(0d1)
  - name: deletefile-map
    language: Prefix
    source: delete_file(<>)
  - name: deletefile-nonexistent
    language: Prefix
    source: |
        import("path")
        
        delete_file(join(path.script_dir, "/nonexistent.txt"))
  - name: deletefile-thr
    language: Prefix
    source: delete_file(async{})
  - name: deletefile-tns
    language: Prefix
    source: delete_file([0d1])
