passing:
  - name: import-alias-qualified-namespace
    language: Prefix
    source: |
        import("path", "helper")
        
        assert(eq(type(helper.temp_file("import-alias-qualified-namespace.txt")), "str"))
  - name: import-alias-type-str
    language: Prefix
    source: |
        import("path", "helper")
        
  - name: import-cached-alias
    language: PowerShell
    source: |
        $helperPath = Join-Path (Split-Path -Parent (Split-Path -Parent $PSScriptRoot)) 'helpers\prefix-input.ps1'
        . $helperPath
        
        $tempDir = New-PrefixTempDir
        $moduleName = 'prefiximport' + ([IO.Path]::GetRandomFileName().Replace('.', ''))
        
        try {
          $markerPath = Join-Path $tempDir 'marker.txt'
          $markerLiteral = $markerPath.Replace('\', '/')
          $programPath = Join-Path $tempDir 'program.pre'
          $modulePath = Join-Path $tempDir "$moduleName.pre"
        
          Set-Content -Path $modulePath -Encoding Ascii -Value @"
        str marker = "$markerLiteral"
        
        if(exist_file(marker)){
            write_file("reloaded", marker)
        } else {
            write_file("loaded", marker)
        }
        
        func str marker_text(){
            return(read_file(marker))
        }
        "@
        
          Set-Content -Path $programPath -Encoding Ascii -Value @"
        import("$moduleName")
        import("$moduleName", "again")
        
        assert(eq($moduleName.marker_text(), "loaded"))
        assert(eq(again.marker_text(), "loaded"))
        "@
        
          $output = & (Get-PrefixExePath) $programPath 2>&1
          if ($LASTEXITCODE -ne 0) {
            $outputText = ($output | ForEach-Object { $_.ToString() }) -join [Environment]::NewLine
            throw "Prefix exited with code $LASTEXITCODE`n$outputText"
          }
        }
        finally {
          Remove-Item -Path $tempDir -Recurse -Force -ErrorAction SilentlyContinue
        }
  - name: import-isolated-top-level
    language: PowerShell
    source: |
        $helperPath = Join-Path (Split-Path -Parent (Split-Path -Parent $PSScriptRoot)) 'helpers\prefix-input.ps1'
        . $helperPath
        
        $tempDir = New-PrefixTempDir
        $moduleName = 'prefiximport' + ([IO.Path]::GetRandomFileName().Replace('.', ''))
        
        try {
          $programPath = Join-Path $tempDir 'program.pre'
          $modulePath = Join-Path $tempDir "$moduleName.pre"
        
          Set-Content -Path $modulePath -Encoding Ascii -Value @'
        int shared = 0d7
        
        func int read_shared(){
            return(shared)
        }
        '@
        
          Set-Content -Path $programPath -Encoding Ascii -Value @"
        int shared = 0d11
        
        import("$moduleName", "helper")
        
        assert(eq(helper.read_shared(), 0d7))
        assert(eq(shared, 0d11))
        "@
        
          $output = & (Get-PrefixExePath) $programPath 2>&1
          if ($LASTEXITCODE -ne 0) {
            $outputText = ($output | ForEach-Object { $_.ToString() }) -join [Environment]::NewLine
            throw "Prefix exited with code $LASTEXITCODE`n$outputText"
          }
        }
        finally {
          Remove-Item -Path $tempDir -Recurse -Force -ErrorAction SilentlyContinue
        }
  - name: import-local-before-stdlib
    language: PowerShell
    source: |
        $helperPath = Join-Path (Split-Path -Parent (Split-Path -Parent $PSScriptRoot)) 'helpers\prefix-input.ps1'
        . $helperPath
        
        $prefixDir = Split-Path -Parent (Split-Path -Parent (Split-Path -Parent $PSScriptRoot))
        $tempDir = New-PrefixTempDir
        $moduleName = 'prefiximport' + ([IO.Path]::GetRandomFileName().Replace('.', ''))
        $stdlibPath = Join-Path $prefixDir "lib\std\$moduleName.pre"
        
        try {
          $programPath = Join-Path $tempDir 'program.pre'
          $localModulePath = Join-Path $tempDir "$moduleName.pre"
        
          Set-Content -Path $localModulePath -Encoding Ascii -Value @'
        func str identify(){
            return("local")
        }
        '@
        
          Set-Content -Path $stdlibPath -Encoding Ascii -Value @'
        func str identify(){
            return("stdlib")
        }
        '@
        
          Set-Content -Path $programPath -Encoding Ascii -Value @"
        import("$moduleName")
        
        assert(eq($moduleName.identify(), "local"))
        "@
        
          $output = & (Get-PrefixExePath) $programPath 2>&1
          if ($LASTEXITCODE -ne 0) {
            $outputText = ($output | ForEach-Object { $_.ToString() }) -join [Environment]::NewLine
            throw "Prefix exited with code $LASTEXITCODE`n$outputText"
          }
        }
        finally {
          Remove-Item -Path $tempDir -Recurse -Force -ErrorAction SilentlyContinue
          Remove-Item -Path $stdlibPath -Force -ErrorAction SilentlyContinue
        }
  - name: import-no-companion-prex
    language: PowerShell
    source: |
        $helperPath = Join-Path (Split-Path -Parent (Split-Path -Parent $PSScriptRoot)) 'helpers\prefix-input.ps1'
        . $helperPath
        
        $tempDir = New-PrefixTempDir
        $moduleName = 'prefiximport' + ([IO.Path]::GetRandomFileName().Replace('.', ''))
        
        try {
          $programPath = Join-Path $tempDir 'program.pre'
          $modulePath = Join-Path $tempDir "$moduleName.pre"
          $pointerPath = Join-Path $tempDir "$moduleName.prex"
        
          Set-Content -Path $modulePath -Encoding Ascii -Value @'
        func str identify(){
            return("source")
        }
        '@
        
          Set-Content -Path $pointerPath -Encoding Ascii -Value 'this companion pointer file must be ignored by import'
        
          Set-Content -Path $programPath -Encoding Ascii -Value @"
        import("$moduleName")
        
        assert(eq($moduleName.identify(), "source"))
        "@
        
          $output = & (Get-PrefixExePath) $programPath 2>&1
          if ($LASTEXITCODE -ne 0) {
            $outputText = ($output | ForEach-Object { $_.ToString() }) -join [Environment]::NewLine
            throw "Prefix exited with code $LASTEXITCODE`n$outputText"
          }
        }
        finally {
          Remove-Item -Path $tempDir -Recurse -Force -ErrorAction SilentlyContinue
        }
  - name: import-package-init
    language: PowerShell
    source: |
        $helperPath = Join-Path (Split-Path -Parent (Split-Path -Parent $PSScriptRoot)) 'helpers\prefix-input.ps1'
        . $helperPath
        
        $tempDir = New-PrefixTempDir
        $packageName = 'prefixpkg' + ([IO.Path]::GetRandomFileName().Replace('.', ''))
        
        try {
          $programPath = Join-Path $tempDir 'program.pre'
          $packageDir = Join-Path $tempDir $packageName
          $initPath = Join-Path $packageDir 'init.pre'
        
          New-Item -ItemType Directory -Path $packageDir | Out-Null
        
          Set-Content -Path $initPath -Encoding Ascii -Value @'
        func str identify(){
            return("package")
        }
        '@
        
          Set-Content -Path $programPath -Encoding Ascii -Value @"
        import("$packageName")
        
        assert(eq($packageName.identify(), "package"))
        "@
        
          $output = & (Get-PrefixExePath) $programPath 2>&1
          if ($LASTEXITCODE -ne 0) {
            $outputText = ($output | ForEach-Object { $_.ToString() }) -join [Environment]::NewLine
            throw "Prefix exited with code $LASTEXITCODE`n$outputText"
          }
        }
        finally {
          Remove-Item -Path $tempDir -Recurse -Force -ErrorAction SilentlyContinue
        }
  - name: import-package-missing-init-errors
    language: PowerShell
    source: |
        $helperPath = Join-Path (Split-Path -Parent (Split-Path -Parent $PSScriptRoot)) 'helpers\prefix-input.ps1'
        . $helperPath
        
        $tempDir = New-PrefixTempDir
        $packageName = 'prefixpkg' + ([IO.Path]::GetRandomFileName().Replace('.', ''))
        
        try {
          $programPath = Join-Path $tempDir 'program.pre'
          $modulePath = Join-Path $tempDir "$packageName.pre"
          $packageDir = Join-Path $tempDir $packageName
        
          New-Item -ItemType Directory -Path $packageDir | Out-Null
        
          Set-Content -Path $modulePath -Encoding Ascii -Value @'
        func str identify(){
            return("module")
        }
        '@
        
          Set-Content -Path $programPath -Encoding Ascii -Value @"
        import("$packageName")
        "@
        
          $result = Invoke-PrefixProgramWithInput -ProgramPath $programPath -InputText ''
          if ($result.ExitCode -eq 0) {
            $outputText = "STDOUT: $(Format-VisibleText $result.Stdout)`nSTDERR: $(Format-VisibleText $result.Stderr)"
            throw "Expected import($packageName) to fail when the package directory has no init.pre`n$outputText"
          }
        }
        finally {
          Remove-Item -Path $tempDir -Recurse -Force -ErrorAction SilentlyContinue
        }
  - name: import-package-precedence
    language: PowerShell
    source: |
        $helperPath = Join-Path (Split-Path -Parent (Split-Path -Parent $PSScriptRoot)) 'helpers\prefix-input.ps1'
        . $helperPath
        
        $tempDir = New-PrefixTempDir
        $packageName = 'prefixpkg' + ([IO.Path]::GetRandomFileName().Replace('.', ''))
        
        try {
          $programPath = Join-Path $tempDir 'program.pre'
          $modulePath = Join-Path $tempDir "$packageName.pre"
          $packageDir = Join-Path $tempDir $packageName
          $initPath = Join-Path $packageDir 'init.pre'
        
          New-Item -ItemType Directory -Path $packageDir | Out-Null
        
          Set-Content -Path $modulePath -Encoding Ascii -Value @'
        func str identify(){
            return("module")
        }
        '@
        
          Set-Content -Path $initPath -Encoding Ascii -Value @'
        func str identify(){
            return("package")
        }
        '@
        
          Set-Content -Path $programPath -Encoding Ascii -Value @"
        import("$packageName")
        
        assert(eq($packageName.identify(), "package"))
        "@
        
          $output = & (Get-PrefixExePath) $programPath 2>&1
          if ($LASTEXITCODE -ne 0) {
            $outputText = ($output | ForEach-Object { $_.ToString() }) -join [Environment]::NewLine
            throw "Prefix exited with code $LASTEXITCODE`n$outputText"
          }
        }
        finally {
          Remove-Item -Path $tempDir -Recurse -Force -ErrorAction SilentlyContinue
        }
  - name: import-qualified-namespace
    language: Prefix
    source: |
        import("path")
        
        assert(eq(type(path.temp_file("import-qualified-namespace.txt")), "str"))
  - name: import-qualified-only
    language: PowerShell
    source: |
        $helperPath = Join-Path (Split-Path -Parent (Split-Path -Parent $PSScriptRoot)) 'helpers\prefix-input.ps1'
        . $helperPath
        
        $tempDir = New-PrefixTempDir
        $moduleName = 'prefiximport' + ([IO.Path]::GetRandomFileName().Replace('.', ''))
        
        try {
          $programPath = Join-Path $tempDir 'program.pre'
          $modulePath = Join-Path $tempDir "$moduleName.pre"
        
          Set-Content -Path $modulePath -Encoding Ascii -Value @'
        int hidden = 0d7
        '@
        
          Set-Content -Path $programPath -Encoding Ascii -Value @"
        import("$moduleName")
        
        assert(eq(hidden, 0d7))
        "@
        
          $result = Invoke-PrefixProgramWithInput -ProgramPath $programPath -InputText ''
          if ($result.ExitCode -eq 0) {
            $outputText = "STDOUT: $(Format-VisibleText $result.Stdout)`nSTDERR: $(Format-VisibleText $result.Stderr)"
            throw "Expected unqualified module binding access to fail after import($moduleName)`n$outputText"
          }
        }
        finally {
          Remove-Item -Path $tempDir -Recurse -Force -ErrorAction SilentlyContinue
        }
  - name: import-return-false
    language: Prefix
    source: |
        assert(eq(type(import("path")), "bool"))
        refute(import("path"))
  - name: import-stdlib-before-userlib
    language: PowerShell
    source: |
        $helperPath = Join-Path (Split-Path -Parent (Split-Path -Parent $PSScriptRoot)) 'helpers\prefix-input.ps1'
        . $helperPath
        
        $prefixDir = Split-Path -Parent (Split-Path -Parent (Split-Path -Parent $PSScriptRoot))
        $tempDir = New-PrefixTempDir
        $moduleName = 'prefiximport' + ([IO.Path]::GetRandomFileName().Replace('.', ''))
        $stdlibPath = Join-Path $prefixDir "lib\std\$moduleName.pre"
        $userlibPath = Join-Path $prefixDir "lib\usr\$moduleName.pre"
        
        try {
          $programPath = Join-Path $tempDir 'program.pre'
        
          Set-Content -Path $stdlibPath -Encoding Ascii -Value @'
        func str identify(){
            return("stdlib")
        }
        '@
        
          Set-Content -Path $userlibPath -Encoding Ascii -Value @'
        func str identify(){
            return("userlib")
        }
        '@
        
          Set-Content -Path $programPath -Encoding Ascii -Value @"
        import("$moduleName")
        
        assert(eq($moduleName.identify(), "stdlib"))
        "@
        
          $output = & (Get-PrefixExePath) $programPath 2>&1
          if ($LASTEXITCODE -ne 0) {
            $outputText = ($output | ForEach-Object { $_.ToString() }) -join [Environment]::NewLine
            throw "Prefix exited with code $LASTEXITCODE`n$outputText"
          }
        }
        finally {
          Remove-Item -Path $tempDir -Recurse -Force -ErrorAction SilentlyContinue
          Remove-Item -Path $stdlibPath -Force -ErrorAction SilentlyContinue
          Remove-Item -Path $userlibPath -Force -ErrorAction SilentlyContinue
        }
  - name: import-type-str
    language: Prefix
    source: |
        import("path")
        
failing:
  - name: import-alias-type-bool
    language: Prefix
    source: 'import("path", true)'
  - name: import-alias-type-flt
    language: Prefix
    source: 'import("path", 0d1.0)'
  - name: import-alias-type-func
    language: Prefix
    source: 'import("path", LAMBDA bool: (){})'
  - name: import-alias-type-int
    language: Prefix
    source: 'import("path", 0d1)'
  - name: import-alias-type-map
    language: Prefix
    source: 'import("path", <>)'
  - name: import-alias-type-thr
    language: Prefix
    source: 'import("path", async{})'
  - name: import-alias-type-tns
    language: Prefix
    source: 'import("path", [0d1])'
  - name: import-arity-three
    language: Prefix
    source: 'import("path", "helper", extra)'
  - name: import-arity-zero
    language: Prefix
    source: import()
  - name: import-missing-module
    language: Prefix
    source: import("prefiximportmissingmoduletest")
  - name: import-type-bool
    language: Prefix
    source: import(true)
  - name: import-type-flt
    language: Prefix
    source: import(0d1.0)
  - name: import-type-func
    language: Prefix
    source: 'import(LAMBDA bool: (){})'
  - name: import-type-int
    language: Prefix
    source: import(0d1)
  - name: import-type-map
    language: Prefix
    source: import(<>)
  - name: import-type-thr
    language: Prefix
    source: import(async{})
  - name: import-type-tns
    language: Prefix
    source: import([0d1])
