passing:
  - name: import_path-alias-qualified-namespace
    language: Prefix
    source: |
        IMPORT("path")
        
        STR tests_dir = path.BASEPATH(path.BASEPATH(path.script_dir))
        STR helper_path = JOIN(tests_dir, "/helpers/import_path_basic_target.pre")
        
        REFUTE(IMPORT_PATH(helper_path, "helper"))
        ASSERT(EQ(helper.identify(), "basic"))
  - name: import_path-alias-type-str
    language: Prefix
    source: |
        IMPORT("path")
        
        STR tests_dir = path.BASEPATH(path.BASEPATH(path.script_dir))
        STR helper_path = JOIN(tests_dir, "/helpers/import_path_basic_target.pre")
        
        IMPORT_PATH(helper_path, "helper")
        
  - name: import_path-cached-alias
    language: Prefix
    source: |
        IMPORT("path")
        
        STR tests_dir = path.BASEPATH(path.BASEPATH(path.script_dir))
        STR helper_path = JOIN(tests_dir, "/helpers/import_path_cache_target.pre")
        STR marker_path = path.TEMPFILE("prefix-import-path-cache-target.txt")
        
        IF(EXISTFILE(marker_path)){
            ASSERT(DELETEFILE(marker_path))
        }
        
        REFUTE(IMPORT_PATH(helper_path))
        REFUTE(IMPORT_PATH(helper_path, "again"))
        
        STR default_view = import_path_cache_target.marker_text()
        STR alias_view = again.marker_text()
        
        ASSERT(DELETEFILE(marker_path))
        ASSERT(EQ(default_view, "loaded"))
        ASSERT(EQ(alias_view, "loaded"))
  - name: import_path-default-basename
    language: Prefix
    source: |
        IMPORT("path")
        
        STR tests_dir = path.BASEPATH(path.BASEPATH(path.script_dir))
        STR helper_path = JOIN(tests_dir, "/helpers/import_path_basic_target.pre")
        
        REFUTE(IMPORT_PATH(helper_path))
        ASSERT(EQ(import_path_basic_target.identify(), "basic"))
  - name: import_path-equivalent-path-cache
    language: PowerShell
    source: |
        $helperPath = Join-Path (Split-Path -Parent (Split-Path -Parent $PSScriptRoot)) 'helpers\prefix-input.ps1'
        . $helperPath
        
        $tempDir = New-PrefixTempDir
        $moduleName = 'prefiximportpath' + ([IO.Path]::GetRandomFileName().Replace('.', ''))
        
        try {
          $markerPath = Join-Path $tempDir 'marker.txt'
          $programPath = Join-Path $tempDir 'program.pre'
          $modulePath = Join-Path $tempDir "$moduleName.pre"
          $extensionlessPath = Join-Path $tempDir $moduleName
        
          $markerLiteral = $markerPath.Replace('\', '/')
          $moduleLiteral = $modulePath.Replace('\', '\\')
          $extensionlessLiteral = $extensionlessPath.Replace('\', '\\')
        
          Set-Content -Path $modulePath -Encoding Ascii -Value @"
        STR marker = "$markerLiteral"
        
        IF(EXISTFILE(marker)){
            WRITEFILE("reloaded", marker)
        } ELSE {
            WRITEFILE("loaded", marker)
        }
        
        FUNC STR marker_text(){
            RETURN(READFILE(marker))
        }
        "@
        
          Set-Content -Path $programPath -Encoding Ascii -Value @"
        IMPORT_PATH("$extensionlessLiteral")
        IMPORT_PATH("$moduleLiteral", "again")
        
        ASSERT(EQ($moduleName.marker_text(), "loaded"))
        ASSERT(EQ(again.marker_text(), "loaded"))
        "@
        
          $result = Invoke-PrefixProgramWithInput -ProgramPath $programPath -InputText ''
          Assert-PrefixSuccess $result
          Assert-NoErrorOutput $result
        }
        finally {
          Remove-Item -Path $tempDir -Recurse -Force -ErrorAction SilentlyContinue
        }
  - name: import_path-isolated-top-level
    language: Prefix
    source: |
        IMPORT("path")
        
        STR tests_dir = path.BASEPATH(path.BASEPATH(path.script_dir))
        STR helper_path = JOIN(tests_dir, "/helpers/import_path_isolated_target.pre")
        
        INT shared = 0d11
        
        REFUTE(IMPORT_PATH(helper_path, "helper"))
        ASSERT(EQ(helper.read_shared(), 0d7))
        ASSERT(EQ(shared, 0d11))
  - name: import_path-no-companion-prex
    language: Prefix
    source: |
        IMPORT("path")
        
        STR tests_dir = path.BASEPATH(path.BASEPATH(path.script_dir))
        STR helper_path = JOIN(tests_dir, "/helpers/import_path_prex_target.pre")
        
        REFUTE(IMPORT_PATH(helper_path))
        ASSERT(EQ(import_path_prex_target.identify(), "source"))
  - name: import_path-package-init
    language: PowerShell
    source: |
        $helperPath = Join-Path (Split-Path -Parent (Split-Path -Parent $PSScriptRoot)) 'helpers\prefix-input.ps1'
        . $helperPath
        
        $tempDir = New-PrefixTempDir
        $packageName = 'prefixpkg' + ([IO.Path]::GetRandomFileName().Replace('.', ''))
        
        try {
          $programPath = Join-Path $tempDir 'program.pre'
          $packageDir = Join-Path $tempDir $packageName
          $initPath = Join-Path $packageDir 'init.pre'
        
          New-Item -ItemType Directory -Path $packageDir | Out-Null
        
          Set-Content -Path $initPath -Encoding Ascii -Value @'
        FUNC STR identify(){
            RETURN("package")
        }
        '@
        
          $packageLiteral = $packageDir.Replace('\', '\\')
        
          Set-Content -Path $programPath -Encoding Ascii -Value @"
        IMPORT_PATH("$packageLiteral")
        
        ASSERT(EQ($packageName.identify(), "package"))
        "@
        
          $result = Invoke-PrefixProgramWithInput -ProgramPath $programPath -InputText ''
          Assert-PrefixSuccess $result
          Assert-NoErrorOutput $result
        }
        finally {
          Remove-Item -Path $tempDir -Recurse -Force -ErrorAction SilentlyContinue
        }
  - name: import_path-package-missing-init-errors
    language: PowerShell
    source: |
        $helperPath = Join-Path (Split-Path -Parent (Split-Path -Parent $PSScriptRoot)) 'helpers\prefix-input.ps1'
        . $helperPath
        
        $tempDir = New-PrefixTempDir
        $packageName = 'prefixpkg' + ([IO.Path]::GetRandomFileName().Replace('.', ''))
        
        try {
          $programPath = Join-Path $tempDir 'program.pre'
          $modulePath = Join-Path $tempDir "$packageName.pre"
          $packageDir = Join-Path $tempDir $packageName
        
          New-Item -ItemType Directory -Path $packageDir | Out-Null
        
          Set-Content -Path $modulePath -Encoding Ascii -Value @'
        FUNC STR identify(){
            RETURN("module")
        }
        '@
        
          $packageLiteral = $packageDir.Replace('\', '\\')
        
          Set-Content -Path $programPath -Encoding Ascii -Value @"
        IMPORT_PATH("$packageLiteral")
        "@
        
          $result = Invoke-PrefixProgramWithInput -ProgramPath $programPath -InputText ''
          if ($result.ExitCode -eq 0) {
            $outputText = "STDOUT: $(Format-VisibleText $result.Stdout)`nSTDERR: $(Format-VisibleText $result.Stderr)"
            throw "Expected IMPORT_PATH($packageLiteral) to fail when the package directory has no init.pre`n$outputText"
          }
        }
        finally {
          Remove-Item -Path $tempDir -Recurse -Force -ErrorAction SilentlyContinue
        }
  - name: import_path-return-false
    language: Prefix
    source: |
        IMPORT("path")
        
        STR tests_dir = path.BASEPATH(path.BASEPATH(path.script_dir))
        STR helper_path = JOIN(tests_dir, "/helpers/import_path_basic_target.pre")
        
        ASSERT(EQ(TYPE(IMPORT_PATH(helper_path)), "BOOL"))
        ASSERT(NOT(IMPORT_PATH(helper_path)))
failing:
  - name: import_path-alias-type-bool
    language: Prefix
    source: |
        IMPORT("path")
        
        STR tests_dir = path.BASEPATH(path.BASEPATH(path.script_dir))
        STR helper_path = JOIN(tests_dir, "/helpers/import_path_basic_target.pre")
        
        IMPORT_PATH(helper_path, TRUE)
  - name: import_path-alias-type-flt
    language: Prefix
    source: |
        IMPORT("path")
        
        STR tests_dir = path.BASEPATH(path.BASEPATH(path.script_dir))
        STR helper_path = JOIN(tests_dir, "/helpers/import_path_basic_target.pre")
        
        IMPORT_PATH(helper_path, 0d1.0)
  - name: import_path-alias-type-func
    language: Prefix
    source: |
        IMPORT("path")
        
        STR tests_dir = path.BASEPATH(path.BASEPATH(path.script_dir))
        STR helper_path = JOIN(tests_dir, "/helpers/import_path_basic_target.pre")
        
        IMPORT_PATH(helper_path, LAMBDA BOOL: (){})
  - name: import_path-alias-type-int
    language: Prefix
    source: |
        IMPORT("path")
        
        STR tests_dir = path.BASEPATH(path.BASEPATH(path.script_dir))
        STR helper_path = JOIN(tests_dir, "/helpers/import_path_basic_target.pre")
        
        IMPORT_PATH(helper_path, 0d1)
  - name: import_path-alias-type-map
    language: Prefix
    source: |
        IMPORT("path")
        
        STR tests_dir = path.BASEPATH(path.BASEPATH(path.script_dir))
        STR helper_path = JOIN(tests_dir, "/helpers/import_path_basic_target.pre")
        
        IMPORT_PATH(helper_path, <>)
  - name: import_path-alias-type-thr
    language: Prefix
    source: |
        IMPORT("path")
        
        STR tests_dir = path.BASEPATH(path.BASEPATH(path.script_dir))
        STR helper_path = JOIN(tests_dir, "/helpers/import_path_basic_target.pre")
        
        IMPORT_PATH(helper_path, ASYNC{})
  - name: import_path-alias-type-tns
    language: Prefix
    source: |
        IMPORT("path")
        
        STR tests_dir = path.BASEPATH(path.BASEPATH(path.script_dir))
        STR helper_path = JOIN(tests_dir, "/helpers/import_path_basic_target.pre")
        
        IMPORT_PATH(helper_path, [0d1])
  - name: import_path-arity-three
    language: Prefix
    source: 'IMPORT_PATH("g:/missing.pre", "helper", extra)'
  - name: import_path-arity-zero
    language: Prefix
    source: IMPORT_PATH()
  - name: import_path-missing-file
    language: Prefix
    source: |
        IMPORT("path")
        
        STR tests_dir = path.BASEPATH(path.BASEPATH(path.script_dir))
        STR missing_path = JOIN(tests_dir, "/helpers/import_path_missing_target.pre")
        
        IMPORT_PATH(missing_path)
  - name: import_path-qualified-only
    language: Prefix
    source: |
        IMPORT("path")
        
        STR tests_dir = path.BASEPATH(path.BASEPATH(path.script_dir))
        STR helper_path = JOIN(tests_dir, "/helpers/import_path_hidden_target.pre")
        
        IMPORT_PATH(helper_path)
        
        ASSERT(EQ(hidden, 0d7))
  - name: import_path-type-bool
    language: Prefix
    source: IMPORT_PATH(TRUE)
  - name: import_path-type-flt
    language: Prefix
    source: IMPORT_PATH(0d1.0)
  - name: import_path-type-func
    language: Prefix
    source: 'IMPORT_PATH(LAMBDA BOOL: (){})'
  - name: import_path-type-int
    language: Prefix
    source: IMPORT_PATH(0d1)
  - name: import_path-type-map
    language: Prefix
    source: IMPORT_PATH(<>)
  - name: import_path-type-thr
    language: Prefix
    source: IMPORT_PATH(ASYNC{})
  - name: import_path-type-tns
    language: Prefix
    source: IMPORT_PATH([0d1])
