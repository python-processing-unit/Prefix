passing:
  - name: len
    language: Prefix
    source: |
        assert(eq(len(0d1), 0d1))
        assert(eq(len("prefix"), 0d1))
        assert(eq(len(0d1, "prefix"), 0d2))
        assert(eq(len(0b1, "prefix", -0xA), 0d3))
        assert(eq(BASE(len(0b1, "prefix", -0xA)), 0d16))
failing:
  - name: len-unsupported
    language: Prefix
    source: len([0d1])
