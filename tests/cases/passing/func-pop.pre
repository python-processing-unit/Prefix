INT pop_target = 0d8

FUNC INT take_and_delete(){
    POP("pop_target")
}

ASSERT(EQ(take_and_delete(), 0d8))
BOOL pop_deleted = FALSE
TRY{
    pop_target
} CATCH(err){
    pop_deleted = TRUE
}
ASSERT(pop_deleted)
