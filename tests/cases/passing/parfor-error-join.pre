TNS: seen = [0d0, 0d0, 0d0]
BOOL: caught = FALSE

TRY{
    PARFOR(i, 0d3){
        seen[i] = i
        IF(EQ(i, 0d2)){
            ASSERT(FALSE)
        }
    }
    ASSERT(FALSE)
} CATCH {
    caught = TRUE
}

ASSERT(caught)
ASSERT(EQ(seen, [0d1, 0d2, 0d3]))
