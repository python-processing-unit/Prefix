BOOL sample_bool_true = TRUE
INT sample_int = -0d7
FLT sample_flt = 0d3.5
STR sample_str = "prefix"
TNS sample_tns = [0d1, 0d2]
MAP sample_map = <"n" = 0d1>

FUNC INT sample_func(INT value){
    RETURN(value)
}

THR(sample_thr){}

REFUTE(ISTNS(sample_bool_true))
REFUTE(ISTNS(sample_int))
REFUTE(ISTNS(sample_flt))
REFUTE(ISTNS(sample_str))
ASSERT(ISTNS(sample_tns))
REFUTE(ISTNS(sample_map))
REFUTE(ISTNS(sample_func))
REFUTE(ISTNS(sample_thr))
