INT left_dec = 0d2
INT right_bin = 0b11
INT wide_hex = 0x10

ASSERT(EQ(ADD(left_dec, right_bin), 0d5))
ASSERT(EQ(BASE(ADD(left_dec, right_bin)), 0d10))

ASSERT(EQ(ADD(wide_hex, left_dec), 0d18))
ASSERT(EQ(BASE(ADD(wide_hex, left_dec)), 0d16))
