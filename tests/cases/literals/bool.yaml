passing:
  - name: bool
    language: Prefix
    source: |
        bool truthy = TRUE
        bool falsy = FALSE
        
        ASSERT(truthy)
        REFUTE(falsy)
failing:
  - name: bool-lowercase
    language: Prefix
    source: ASSERT(true)
