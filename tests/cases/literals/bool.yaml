passing:
  - name: bool
    language: Prefix
    source: |
        bool truthy = true
        bool falsy = false
        
        assert(truthy)
        refute(falsy)
  - name: bool-lowercase
    language: Prefix
    source: assert(true)
