This is the canonical implementation of the Prefix programming language (originally written in Python) in C17, targeting MSVC (VS2026).

The versioning system used is [SemVer 2.0](https://semver.org/).
